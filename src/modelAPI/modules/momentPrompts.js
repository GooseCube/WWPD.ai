export const initialPrompt = (userMessage) => {
  return `You are a prompt engineer creating prompts for a LLM simulation. You are very creative and an excellent writer. Given the message below create a prompt to have the model provide some context for the simulation: ${idea}`;
};
